[tox]
envlist = py27,py34,py35,flake8,coverage-report

[testenv]
deps =
  coverage
  pytest
commands = coverage run --parallel -m pytest tests

[testenv:flake8]
basepython = python3.5
deps =
  flake8
  flake8-import-order
commands = flake8 src tests setup.py

[testenv:coverage-report]
deps = coverage
skip_install = true
commands = 
  coverage combine
  coverage report

[testenv:wheel]
deps = wheel
commands = python setup.py bdist_wheel
