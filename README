blog.condi.me
-------------

This is a reimplementation of my blog in twisted.

Getting started
---------------

1/. Run the blog engine

  $ git clone https://github.com/bencord0/blog -b python-twisted
  $ git clone https://github.com/bencord0/blogposts

  $ (cd blog; ./build.sh)
  $ (cd blogposts; ../blog/run.sh)

2/. Report inconsistencies with the live site, and make suggestions.

Using the Dockerfile
--------------------

  $ ./build.sh && ./release.sh
  $ docker run -p 8000:8000 -v $PWD/../blogposts:/posts:ro -w /posts -t blog
